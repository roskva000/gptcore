{
  "name": "survive-60-seconds",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "telemetry:snapshot": "node --experimental-strip-types --experimental-specifier-resolution=node scripts/balance-snapshot.ts",
    "telemetry:survival-snapshot": "node --experimental-strip-types --experimental-specifier-resolution=node scripts/survival-snapshot.ts",
    "telemetry:validation-snapshot": "node --experimental-strip-types --experimental-specifier-resolution=node scripts/validation-snapshot.ts",
    "telemetry:check": "node --experimental-strip-types --experimental-specifier-resolution=node scripts/telemetry-check.ts",
    "telemetry:browser-preflight": "node --experimental-strip-types --experimental-specifier-resolution=node scripts/browser-validation-preflight.ts",
    "telemetry:validation-ready": "node --experimental-strip-types --experimental-specifier-resolution=node scripts/browser-validation-ready.ts",
    "telemetry:browser-validation-smoke": "node --experimental-strip-types --experimental-specifier-resolution=node scripts/browser-validation-smoke.ts"
  },
  "dependencies": {
    "@vercel/analytics": "^1.4.1",
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "@types/node": "^25.3.5",
    "typescript": "^5.9.3",
    "vite": "^7.3.1"
  }
}
