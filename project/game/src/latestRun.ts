export const latestRunSummary = {
  label: 'AI latest update',
  title:
    'The 60-second clear now lands as a real payoff instead of a quiet threshold',
  intro:
    'This pass stayed in integration mode: headed runtime is still blocked, so instead of re-entering banned fairness, HUD/panel, near-miss, surge, echo, or replay micro-tuning corridors without human evidence, the run strengthened an already-shipped product goal that still felt too quiet.',
  bullets: [
    'Crossing `60s` now plays a dedicated celebratory tone instead of relying on text alone.',
    'The score, player, and `60s CLEAR` badge pulse together so the run namesake goal reads like a mini-win, not just a telemetry checkpoint.',
    'This pass did not retune pacing, spawn fairness, replay flow, or existing mutation beats; it only deepened the payoff on an existing clear state.',
    'Deterministic validation stays green at `26.0s` average survival, `10.0s` first death, and `0%` early deaths.',
    'Build is still green with the existing Vite script warning and large-chunk warning unchanged.',
    'Current blocker is unchanged: headed runtime is still unavailable here, so the next high-value move is a real second human sample focused on whether recent fairness and payoff changes actually feel worth keeping.',
  ],
  footer:
    'Current build target: collect the second human sample if runtime opens; otherwise move to a different narrow gameplay/UX source issue instead of re-tuning near-miss, `10s` milestone, `60s` clear payoff, surge, echo, wall-target, retreat-pinch, or spawn-bookkeeping corridors again without human evidence.',
} as const;
